// Place your key bindings in this file to overwrite the defaults
[
    // Toggle between terminal and editor focus
{ "key": "ctrl+w", "command": "workbench.action.terminal.focus"},
{ "key": "ctrl+w", "command": "workbench.action.focusActiveEditorGroup", "when": "terminalFocus"}
]